# F1Tenth 強化学習 実験推移レポート

## 概要と目的
本レポートは、F1Tenthシミュレーション環境における強化学習（PPOアルゴリズム）を用いた自律走行エージェント育成プロジェクトの、実験経緯、目的、および得られた知見をまとめたものである。
目的は「安定した周回（完走）能力の獲得」と「走行速度の向上」の両立であり、試行錯誤の過程を記録している。

## 実験フェーズごとの詳細経緯

### フェーズ1：観測空間の複雑さとペナルティの罠（ベースライン 〜 EXP-06）
初期のベースラインモデル（PPO_27、500万ステップ学習）では、1082次元の複雑なLidar観測空間に対してAIのアクションのばらつき（`std`）が3.43と発散し、完走率は0%のままであった。

*   **EXP-01 (`log_std_init=-1.0`)**
    *   **目的**: 行動の極端な発散（std의 異常値）を初期段階から抑え込むこと。
    *   **結果**: 発散は1.92へと半減したが、依然として予測不能な動きが続き完走できず(0%)。
*   **EXP-03 (`COLLISION=-200, SURVIVAL=0.1`)**
    *   **経緯**: `REWARD_COLLISION = -1000.0` という高すぎるペナルティにより、AIが「動かない手（最低速度）」を選ぶ局所解に陥っていることが判明したため、ペナルティを緩和し、生存することへのインセンティブを追加した。
    *   **結果**: 完走率**30%**を達成。しかし、行動（`std`）が20.2という異常な発散（パニック状態）を示しており、実用には耐えなかった。
*   **EXP-05 & EXP-06 (`INCLUDE_LIDAR_RESIDUAL=False`)**
    *   **目的**: EXP-01の安定化とEXP-03の報酬緩和のハイブリッド化（EXP-05）。それでも発散が収まらないため、ノイズの原因となりうるLidarの過去との差分（Residual）入力を無効化して観測をシンプルにした（EXP-06）。
    *   **結果**: 完走は逃した（0%）ものの、EXP-06において`std`が0.93へと劇的に改善。ここで「行動の安定化」という第一の課題をクリアした。

### フェーズ2：モデルの表現力向上と安全性の過剰学習（EXP-07 〜 EXP-10）
行動の安定化に成功したため、次はコースを走り切るための「知能（ネットワークの表現力）」と「姿勢制御」に焦点を当てた。

*   **EXP-07 (`NET_ARCH=[128, 128]`)**
    *   **目的**: 固定的な単純行動を脱却するため、ニューラルネットワークの隠れ層を拡大し、より複雑な環境認識を可能にする。
    *   **結果**: `std`の安定を保ったまま、**完走率25%**を達成。表現力向上の有効性を確認し、初めて理にかなったゴール到達を果たした。
*   **EXP-08 〜 EXP-10 (安全性と進行方向の報酬調整)**
    *   **経緯と目的**: 完走率をさらに引き上げるため、安全マージン（`SAFETY_WEIGHT=1.5`）や左右へのブレに対するペナルティ（EXP-09）、そしてコース中央を前進することへの強いボーナス（EXP-10）を次々にテストした。
    *   **結果**: いずれの実験も**完走率0%**へと逆戻りした。AIは安全を過剰に意識するか、特定の箇所の突破方法を見出せず、どの報酬設定でも同じ壁の同じ箇所で衝突し続ける停滞状態に陥った。これは単なる探索不足ではなく、「最善の諦め」であると分析された。

### フェーズ3：根本的制約の発見とブレイクスルー（EXP-11 〜 EXP-13）
報酬関数をいくら調整しても特定のコーナーでぶつかる現象を疑い、AIの知識ではなくシミュレータの「物理エンジンと環境パラメータ」を精査した。

*   **EXP-11 & EXP-12 (`Correct Size 0.465x0.19`)**
    *   **経緯と目的**: これまで完走できなかった根本原因は、シミュレーション上の機体サイズがデフォルト設定（0.58m x 0.31m）のままであり、実際のF1Tenth車両よりも巨大であったためと判明。AIの表現力不足ではなく、物理的に窮屈な状態で走らされていた。EXP-12で実際の寸法（0.465m x 0.19m）へ補正し走行をテストした。
    *   **結果**: 寸法を直しても完走率は0%。プレイ動画を重ねて分析した結果、コーナーを曲がろうとする意思は見られるが外側へ膨らんでしまうことを確認。ここから、**`MIN_SPEED = 1.0 m/s` の下限設定が高すぎ**て、物理的に自力で曲がり切れないことが真のボトルネックであると特定した。
*   **EXP-13 (`Brake Before Corner`)**
    *   **目的**: 最低速度（`MIN_SPEED`）の制約を 0.3 m/s へと引き下げ、コーナー手前でAIが主体的にブレーキ（減速）をかけられるようにする。
    *   **結果**: ついに**完走率70.0%**という劇的な改善（ブレイクスルー）を達成。AIは安全第一の走行を学習し、平均速度0.29m/sにて各コーナーを丁寧にクリアする「減速制御」の挙動を獲得した。

### フェーズ4：安定走行から高速化への挑戦（EXP-14 〜 EXP-17）
現在は安定した完走能力を土台として、タイムの短縮（速度向上）を目指すフェーズに入っているが、速度への過剰な適応が安定性を損なう「学習の揺り戻し」に直面している。

*   **EXP-14 (`Gradual Speed Up`)**
    *   **目的**: EXP-13で獲得した「コーナーでの減速制御（ブレーキ力）」を確実に維持しつつ、直線や緩やかなカーブではスロットルを大きく開くように促す。
    *   **結果**: **完走率0%**。速度報酬と進行報酬を強化したが、特定の箇所で確実に衝突する「自信に満ちた失敗（局所解）」に陥った。
*   **EXP-15 (`Prudent Speed`)**
    *   **目的**: EXP-14の失敗を受け、報酬のバランスを再調整。ノイズの可能性があるΔLiDAR（Residual）を再度オフにし、慎重な速度向上を図る。
    *   **結果**: **完走率0%**。依然として外壁に進入する癖が抜けず、学習が局所解から脱却できなかった。
*   **EXP-16 (`EXP-13 Reproduction`)**
    *   **目的**: 直近の失敗が「報酬設計」によるものか「学習の受け皿」によるものかを切り分けるため、完走率70%を記録したEXP-13を完全再現する。
    *   **結果**: **完走率50%**。EXP-13ほどの数値ではないものの、完走能力の再現を確認。これをベースラインとして再設定。
*   **EXP-17 (`Stable Speed Up`)**
    *   **目的**: EXP-16をベースに、平均速度を0.3m/sから引き上げるための極めて微小な調整を行う。
    *   **結果**: **完走率0%**。平均速度は0.37m/sまで向上したが、安定性が崩れ微小な速度向上が衝突を招く結果となった。

### フェーズ5：観測解像度の向上と制御の適正化（EXP-18 〜 実施中）
物理的な微調整では限界が見えたため、AIが環境を認識する「解像度」そのものを改善し、より精密な制御を可能にするアプローチへ移行。

*   **EXP-18 (`High Res & Steering Sensitivity`)**
    *   **目的**: LiDARのダウンサンプリング係数を 10 から 4 へ変更（108点→270点）し、情報の密度を高める。また、ステアリング感度をシミュレータの物理限界（0.41）に合わせることで、意図通りの旋回を可能にする。
    *   **現状**: モデルのトレーニングが完了（10M steps）。初動の動画確認では、より滑らかな軌道を描く兆候が見られる。

### フェーズ6：並列学習と安定完走の極致（EXP-22 〜 EXP-25）
物理制約をクリアした後、学習の効率と安定性を追求。ここでプロジェクトは一つの頂点に達した。

*   **EXP-22 (`Parallel Training & Sensitivity`)**
    *   **目的**: SubprocVecEnvによる8環境並列化を導入し、学習効率を大幅アップ。ステアリング感度を1.0に復帰させ、機敏な回避を可能にした。
*   **EXP-25 (`100% Success Benchmark`)**
    *   **結果**: ついに**完走率100%**を達成。平均速度0.32m/s、std 0.16という「迷いのない、極めて安定した走り」を確立。これが実質的なプロジェクトの「完成形ベースライン」となった。

### フェーズ7：実機マップの衝撃とカリキュラム学習（EXP-31 〜 EXP-33）
実機のCartographerで構築した `testmap-0416` への移行。これまでの「広くて走りやすいコース」とは別次元の地獄に直面した。

*   **EXP-31 & EXP-32 (`The Narrow Wall`)**
    *   **事実**: `testmap-0416` は道幅がマシンのわずか1.8倍。少しのズレが即死を招く。
    *   **結果**: 完走率0%。EXP-25の知識をもってしても、狭い直線と鋭角コーナーで即座に衝突。
*   **EXP-33 (`Curriculum Learning & 270° Masking`)**
    *   **戦略**: ゼロからのFresh学習を諦め、3フェーズの「カリキュラム学習」を導入。
        1. 広いマップで新報酬（270°LiDARマスク）に慣れる。
        2. 狭いマップを極低速（MAX=1.5）で「まず完走」する。
        3. 徐々に速度を上げる。
    *   **Sim-to-Real**: 実機Hokuyoに合わせてLiDARを270°に制限。
    *   **結果**: 平均生存ステップ数が76から178へ大幅改善。特定の条件下では1400ステップ以上のロングランが可能になった。

### フェーズ9：物理特性の回帰と生存の極致 (EXP-35 〜 EXP-36)
最新の試行では、複雑化したパラメータを一度整理し、極狭環境での「確実な生存」を優先する戦略をとっている。

*   **EXP-35 (`Back to Original Frame`)**
    *   **目的**: EXP-34での仮想車幅（0.23m）やステア感度（1.3）の強化が、かえってAIの運転感覚を狂わせている可能性を考慮し、物理値を実機寸法（0.19m）と標準感度（1.0）に戻して再学習。
    *   **結果**: 挙動の不自然さは解消されたが、依然として最高速度域での安定性が課題。
*   **EXP-36 (`Snail Paced Survival`)**
    *   **目的**: 基本に立ち返り、`MIN_SPEED=0.1`, `MAX_SPEED=0.5` という超低速設定を導入。AIに「どんなに遅くても良いから、壁を避ければ報酬が得られる」という生存本能を徹底的に学習させる。
    *   **期待**: 低速での「絶対完走」を達成し、そこから段階的に速度（MAX_SPEED）を上げていくための最強のベースモデルを作成する。

### フェーズ10：実機仕様への完全移行と新規挑戦（EXP-37 〜）
プロジェクトの更なる Sim-to-Real 精度向上を目指し、AI の観測そのものを実機の物理限界に合わせる抜本的な再定義を行った。

*   **EXP-37 (`270° Sensor Fusion & Fresh Start`)**
    *   **事実**: 従来の 360° LiDAR は後方の壁まで見えており、実機の 270° センサー（Hokuyo URG）との間に情報の乖離（Sim-to-Real Gap）があった。
    *   **戦略**: AI の入力ベクトルそのものを 108 次元から 81 次元（270°相当）に制限。次元変更を伴うため、過去のモデルを継承せず、新規マップ `map-tamoku` にてゼロから学習を開始。
    *   **報酬**: 最も成功した EXP-25 のシンプルロジックをベースとし、前方 2.0m 基準の減速制御を統合。
    *   **期待**: 「見えないものは無視する」という実機に忠実な視界を持たせることで、体育館のような広大かつ特殊な空間における制御の堅牢性を高める。

## 【まとめと今後の知見】
本プロジェクトを通じて得られた最大の知見は以下の通りである。

1.  **物理的制約の早期特定**: `MIN_SPEED` や実機寸法など、AI 以前の物理パラメータがボトルネックになる。
2.  **カリキュラム学習の威力**: 困難な課題にいきなり挑ませず、広いコースから段階的に適応させる（Resume/Transfer Learning）ことが、複雑な地形への唯一の攻略法である。
3.  **仮想的なマージン（Ghost Width）**: 衝突ギリギリの極狭環境では、AI に「実際より大きい自分」を意識させることで、不確定要素（スリップや処理遅延）に対する余裕が生まれる。
4.  **Sim-to-Realの整合性**: 実機スペック（270°スキャン等）を観測および報酬系に最初から組み込むことで、現場での適応をスムーズにする。
5.  **環境統計の重要性**: 広大なマップでは `LIDAR_MEAN` などの分布が極端に変化するため、学習開始前のキャリブレーションとそれに基づいた正規化が学習速度を左右する。

今後は、実機搭載に向けた軽量化（ONNX化）や、動的障害物への対応などを視野に入れ、さらなる高度な自律走行（最短タイムの追求）を目指す。

### フェーズ11：実機同期と観測の高解像度化（EXP-38 〜 EXP-39）
実機（Jetson/Hokuyo）との挙動の一致（Sim-to-Real）を極限まで高めるため、制御周期の同期と観測空間の抜本的な強化を行った。

*   **EXP-38 (`40Hz Sync Control`)**
    *   **目的**: 実機のLiDAR出力およびROSノードの動作周期に合わせて、シミュレーションの制御周波数を 20Hz から **40Hz** へ引き上げ。1ステップあたりの時間を 0.05s から 0.025s に短縮し、より実機に近い時間分解能での制御を学習させる。
    *   **結果**: 挙動がより細やかになり、高速域での制御破綻が減少。
*   **EXP-39 (`High Res & Frame Stacking`)**
    *   **目的**: 狭路での精密な回避と、動的な姿勢変化の把握を両立させる。
    *   **戦略**: 
        1. **LiDAR解像度の倍増**: 1080本から **1440本** (0.25°刻み) へシミュレータのビーム数を増やし、ダウンサンプリング後も 216点 (従来の2倍) の情報を維持。
        2. **フレーム積層 (FRAME_STACK=4)**: 過去の観測を4フレーム分（間引き3により約0.25秒間）積層し、速度ベクトルや相対的な壁の接近速度を認識可能に。
        3. **車両状態の統合**: 現在の速度とステアリング角を直接観測に含め、AIが自身の状態をフィードバックできるようにした。
    *   **成果**: `ppo_10M_exp39_high_res_early_brake.onnx` としてモデルをエクスポート。高解像度化により、壁ギリギリを通る際の安定性が劇的に向上。

### フェーズ12：CNNポリシーの導入と検証（EXP-40 〜 EXP-42）
従来のMLPポリシーから、LiDARの空間的連続性をより直接的に扱うCNN（Conv1D）ポリシーへ移行。

*   **EXP-41 (`CNN Bug Hunt`)**
    *   **結果**: **完全崩壊**。CNN導入直後に学習が成立しない事態が発生。原因分析の結果、①CNNの時間軸が「最新→最古」で物理的因果関係が逆転していたこと、②`ACTION_REPEAT`中のフレームが同一画像で埋まっていたこと、③スポーン位置の僅かな誤差による即死、の3点が特定された。
*   **EXP-42 (`Back to Basics - MLP`)**
    *   **目的**: 問題がCNNにあるのか他にあるのかを切り分けるため、一時的にMLPに回帰。
    *   **結果**: 平均歩数は改善したが、MLP[128,128]では872次元の高解像度データを処理しきれないボトルネックが確認され、CNNの必要性が再確認された。

### フェーズ13：CNNの再構築とマップ最適化（EXP-43 〜 EXP-48）
特定されたバグを修正し、難関マップ `tamoku` および `map_1_0509_145516` への適応を開始。

*   **EXP-43 (`CNN Resurrection`)**
    *   **改善**: `torch.flip` による時間軸修正と、サブステップごとの観測更新を実装。
    *   **結果**: 学習の収束性が劇的に改善し、高解像度LiDARを用いたCNNの有効性を証明。
*   **EXP-46 (`The Spinning Trap`)**
    *   **失敗**: `Gap Follow` 報酬を安易に導入した結果、広い直線で回転し続けてLiDARの最大距離を稼ぐ「報酬ハッキング」が発生。
    *   **対策**: `spin_penalty`（高ステアリング×低速度へのペナルティ）を導入し、不正な局所解を封じ込めた。
*   **EXP-47 & EXP-48 (`Curve Awareness & Map Optimization`)**
    *   **課題**: 直線は安定したが、マシン2台分しかない狭いカーブ入り口で立ち往生する傾向が判明。
    *   **解決策**: 
        1. **斜め前方報酬 (`open_side`)**: 真前だけでなく斜め方向の空間も評価に含め、コーナーの「先」を意識させる。
        2. **統計的正規化**: マップごとの壁距離分布（p50/p75）に基づいた安全スコアの再定義。
        3. **カリキュラムスポーン**: 苦手なカーブ入り口付近からのスポーンを重点的に行うことで、難所を集中学習。

## 【最新の到達点と次なる課題】
現在、プロジェクトは「極狭路における空間認識の精度向上」という最も困難な壁に挑んでいます。CNNの導入とサブステップ観測により、AIは「自分がどちらに動いているか」をより正確に把握できるようになりました。
次のステップでは、現在の安定した生存能力を維持したまま、MAX_SPEEDを段階的に引き上げ、競技レベルのスピードでの完走を目指します。

### フェーズ14：報酬ハッキングの根絶とカーブ走行の明示的学習（EXP-49 〜 実施中）
これまでの実験で発生していた「直進しかしない」または「回転し続ける」という極端なバイアス（報酬ハッキング）を解消し、コースに沿った臨機応変な走行を実現するための抜本的な改修を実施。

*   **EXP-49 (`Counter-Hacking & Explicit Curve Rewards`)**
    *   **目的**: 報酬関数の構造的欠陥による「局所解への収束」を防ぎ、カーブでの適切な減速と旋回を学習させる。
    *   **戦略**: 
        1.  **回転ハッキングの根絶**: 斜め方向の空間を評価していた `open_side` ロジックを廃止。真正面に空間があることのみを評価するように変更し、体を斜めにして回り続けるメリットを完全に消滅させた。
        2.  **カーブステアリング報酬の新設**: 左右の前方距離の差（非対称性）を検知し、道が開いている方向にハンドルを切る行動を直接報酬化。AIに「曲がりきれた」という成功体験を明示的に与える。
        3.  **直進ボーナスの適正化**: ステアリング安定性ボーナスを「直線路（左右が均等）かつ前方が空いている場合」のみに制限。カーブ入口での無理な直進を抑制。
        4.  **スピンペナルティの4倍強化**: 低速度での高ステアリングに対するペナルティを大幅に強化し、回転による「生存報酬稼ぎ」を封殺。
        5.  **観測空間の拡張 (lr_asymmetry)**: 左右前方距離の差分をスカラー特徴として追加。AIが「右が空いている＝右カーブ」という情報を直接認識できるようにした。
        6.  **探索ハイパーパラメータの増強**: `PPO_ENT_COEF` を 0.005 から 0.015 へ引き上げ。探索を増やすことで、直進や回転といった安易な局所解から抜け出しやすくした。
        7.  **戦略的スポーン地点の配置（ユーザーによる手動調整）**: スポーン地点を従来の4点から**13点**へ大幅拡充。カーブの入口、頂点、出口、およびあえて壁に近い「リカバリーが必要な位置」を網羅。
    *   **期待される成果**: 報酬ハッキングによる「嘘の学習」を排除し、コース形状に合わせた柔軟なステアリングと、前方距離に応じた臨機応変なスピード調整能力の獲得。
