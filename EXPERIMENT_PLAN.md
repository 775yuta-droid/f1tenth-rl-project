# F1Tenth RL 実験マスタープラン & AI引き継ぎ書

> [!IMPORTANT]
> **AIアシスタントへの動作指示**:
> あなたはこのプロジェクトの自律型実験担当者です。別PCへの移行後、本ドキュメントを「唯一の真実（Source of Truth）」として扱い、以下の原則に従って実験を代行してください。
> 1. **一変数の原則**: 一度の実験で変更する変数は必ず「一つ」に絞ること。
> 2. **因果関係の尊重**: すでに得られた知見（セクション0）に反する設定に、独断で戻さないこと。
> 3. **多角的評価**: 完走率が向上しても、`std`（行動のばらつき）が 1.0 を超えて発散している場合は「失敗」とみなし、安定化を優先すること。
> 4. **記録の継続**: 実験を終えるごとに本ドキュメントの「実験ログ」テーブルを更新し、判断の根拠を残すこと。

---

## 0. これまでの実験で得られた決定的知見 (Key Findings)
新しい環境で実験を再開・評価する際、以下の知見を前提としてください。

*   **知見1（衝突ペナルティの罠）**: `REWARD_COLLISION = -1000.0` は強すぎる。AIは「動かない（最低速度）のが最も報酬の損失が少ない」という局所解に陥り、速度向上へのインセンティブが完全に消失する。
*   **知見2（行動の発散）**: `log_std_init` をデフォルト（0.0）のまま学習すると、1082次元の複雑な観測空間に対し、行動のばらつき（`std`）が 3.0〜20.0 という異常値まで発散する。これを抑えるには `-1.0` 以下の初期値が必須。
*   **知見3（局所解の性質）**: 完走率が0%のとき、AIは特定の壁の同じ箇所に衝突し続ける。これは探索不足ではなく、不適切な報酬設計による「最善の諦め」である。
*   **知見4（学習効率）**: `explained_variance` が 0.8 未満のモデルは、環境を十分に理解できていない。500万ステップ走らせてもこれが低い場合は、ステップ数を増やすのではなく「報酬関数のシンプル化」または「ネットワークサイズの拡大」が必要。
*   **知見5（物理寸法の重要性）**: f110-v0のデフォルトサイズは 0.58m x 0.31m。`f1_env.py` で `env.sim.agents[0].params` を直接書き换えないと内部に後者が決定的に適用される。必ず内部パラメータを確認すること。
*   **知見7（他マップでの汎用性）**: EXP-16モデルは `levine` マップで完走率 70%、平均速度 1.0m/s を記録。
*   **知見8（極狭路の壁：1.8倍）**: `testmap-0416` のような極狭路（道幅1.8倍）では、センターラインから10cmずれるだけで即死する。報酬のスケール（Survival=0.5に対しCollision=-50程度）と、AIへの「だまし（仮想車幅）」によるマージン確保が不可欠。
*   **知見9（カリキュラム学習）**: 困難な新マップへの適応には「広い地形で新報酬に慣れる（Phase 1）→ 極低速で地形を覚える（Phase 2）」という段階的な Resume 戦略が極めて有効。
*   **知見10（観測空間の定義変更）**: LiDAR のスキャン範囲を 270° に制限する場合、入力次元数が 108 -> 81 に減少するため、既存モデルとの互換性が失われる。Sim-to-Real を優先する場合は Fresh学習が必須。

---

## 1. 環境要件 (Environment)
別PCへ移行する際は、以下の環境が整っていることを確認してください。

- **OS**: Ubuntu 20.04+ (WSL2推奨)
- **Container**: Docker & Docker Compose
- **Simulation**: F1Tenth Gym (RK4 Integrator)
- **RL Framework**: Stable Baselines3 (PPO)

---

## 2. 実験ログと進捗 (Log & Current Status)

| 実験ID | 主な変更点 | 完走率 | 最終`std` | `exp_var` | 判定・所見 |
|---|---|---|---|---|---|
| PPO_27 | ベースライン (5M) | 0% | 3.43 | 0.22 | ✗ 行動が発散。 |
| EXP-01 | `log_std_init=-1.0` | 0% | 1.92 | -0.11 | ✗ 発散は半減したが予測不能に。 |
| EXP-02 | `ENT_COEF=0.003` | — | — | — | スキップ（後に回す） |
| EXP-03 | `COLLISION=-200, SURVIVAL=0.1` | **30%** | **20.2** | 0.41 | 🟡 **完走成功**。ただし行動は異常なパニック。 |
| **EXP-05** | **EXP-01 + EXP-03 (Hybrid)** | 0% | 14.8 | 0.05 | ✗ 生存時間は伸びたが完走できず。 |
| **EXP-06** | `INCLUDE_LIDAR_RESIDUAL=False` | 0% | 0.93 | 0.31 | ✗ 完走できず。ただし行動発散(`std` < 1.0)は大幅に改善。 |
| **EXP-07** | `NET_ARCH=[128, 128]` | **25%**| 1.24 | 0.35 | 🟡 **完走成功(25%)**。`std` は微増したが、表現力向上により初のゴール到達。 |
| **EXP-08** | `SAFETY_WEIGHT=1.5` | 0% | 0.95 | 0.48 | ✗ **完走できず(0%)**。`std` は安定したが、安全マージン強化が逆に衝突を招いた可能性。 |
| **EXP-09** | `左右ペナルティ+SAFETY=1.0` | 0% | 1.2 | 0.58 | ✗ **完走できず(0%)**。`exp_var` は向上したが、左右ペナルティが走行を妨げた可能性。 |
| **EXP-10** | `CenterBonus+ProgWeight=2.0` | 0% | 1.23 | 0.28 | ✗ **完走できず(0%)**。中央ボーナスと強気のProgress設定でも、特定のコーナーを突破できず。 |
| **EXP-11** | `10M steps + LowLR + ExpWall` | 0% | 0.36 | 1.00 | ✗ **完走できず(0%)**。原因判明：物理寸法が 0.58m x 0.31m と巨大だった。 |
| **EXP-12** | `Correct Size (0.465x0.19)` | 0% | 0.30 | 0.997 | ✗ **完走できず(0%)**。平均速度 1.04m/s。動画でコーナー外側への膨らみを確認。**結論：MIN_SPEEDが高すぎ**。 |
| **EXP-13** | `Brake Before Corner` | **70%** | **0.69** | **0.99** | ✅ **完走成功(70.0%)**。ブレーキ許可と早期減速により安定完走。平均速度 0.29m/s。 |
| **EXP-14** | `Gradual Speed Up` | 0% | 0.11 | 0.875 | ✗ **完全失敗(0%)**。自信を持って衝突する局所解。 |
| **EXP-15** | `Prudent Speed` | 0% | 0.60 | 0.977 | ✗ **失敗(0%)**。全ポイントから外壁に進入。学習局所解が固定。 |
| **EXP-16** | `EXP-13 完全再現` | **50%** | **0.61** | **0.997** | 🟡 **再現性確認**。50%完走。ベースラインとして信頼可能。 |
| **EXP-17** | `Stable Speed Up` | 0% | 0.51 | 0.994 | ✗ **失敗(0%)**。平均速度は0.37m/sへ向上したが不安定に。 |
| **EXP-18** | `High Res & Steering` | 0% | 0.51 | 0.996 | ✗ **失敗(0%)**。解像度を上げてもmy_mapでは完走できず。 |
| **EXP-19** | `Guided High-Res` | 0% | 0.37 | 0.998 | ✗ **失敗(0%)**。蛇行（スネーキング）が発生し衝突。 |
| **EXP-20** | `Steer Delta Control` | 0% | 0.28 | 0.996 | ✗ **失敗(0%)**。蛇行は減ったが、コーナーでの反応力が不足。 |
| **EXP-21** | `Frame Stacking` | 0% | 0.28 | 0.998 | ✗ **失敗(0%)**。走行は滑らかになったが、低速不安定で衝突。STEER_SENSITIVITY=0.41が根本原因と特定。 |
| **EXP-22** | `Parallel + Back to Basics` | 30% | 0.29 | 0.998 | 🟡 **部分成功(30%)**。STEER=1.0復帰で完走率回復。mean()が壁危険信号を薄めていると分析。 |
| **EXP-23** | `min() + Frame Stack` | 55% | 0.28 | 0.998 | ✅ **成功(55%)**。ベースライン(50%)超え。早期衝突が特定地点に集中。 |
| **EXP-24** | `Spawn Masking` | 80% | 0.29 | 0.998 | ✅ **成功(80%)**。過去最高記録更新。報酬累積マイナスが課題。 |
| **EXP-25** | `Fast & Stable` | 100% | 0.30 | 0.998 | 🏆 **大成功(100%)**。特定スポーン除外とブレーキ緩和で完璧な完走を達成。 |
| **EXP-26** | `Top Speed Challenge` | 0% | 0.96 | 0.998 | ✗ **失敗(0%)**。目標速度に到達したが、転舵不全で完走できず。 |
| **EXP-27** | `Exploration Resume` | 0% | 0.96 | 0.998 | ✗ **失敗(0%)**。探索強化でも高速域の壁を越えられず。 |
| **EXP-28** | `Precision Turn` | 0% | 0.96 | 0.998 | ✗ **失敗(0%)**。**動画確認: 直進して壁衝突**。ステア連動ペナルティが「大舵角=不利」を学習させ、AIがハンドルを切らなくなった逆効果。 |
| **EXP-29** | `Straight Fix` | 0% | 20.4 | 0.992 | ✗ **失敗(0%)**。stdが 0.37→**20.4へ完全発散**。ステアペナルティ削除により行動制約がなくなり瞎ランダム状態。平均速度 0.49m/s(最低速度附近)。 |
| **EXP-30** | `Gradual Speed Up (Resume)` | — | — | — | 実施予定: EXP-25からResume + MIN_SPEED=0.5 + MAX_SPEED=3.0。「曲がる能力」を保持したまま高速化。 |
| **EXP-31** | `New Map Fresh Flight` | — | — | — | 実施準備中: 新マップ `testmap-0416` でのFresh学習。Rewards多段階化を適用。 |
| **EXP-32** | `Resume EXP-25 + 270° Mask` | 0% | 0.27 | 0.992 | ✗ **失敗(0%)**。平均ステップ数は122へ向上したが完走不可。 |
| **EXP-33** | `3-Phase Curriculum` | 0% | 0.18 | 0.987 | 🟡 **部分成功**。特定位置なら1400歩(ほぼ1周)可能に。 |
| **EXP-34** | `Ghost Width + Strict Center`| — | — | — | 仮想車幅(0.23m)と強力なセンター報酬(x20)の合わせ技。 |
| **EXP-35** | `Back to Original Frame` | — | — | — | 物理特性を標準(0.19m/1.0)に戻し感覚リセット。 |
| **EXP-36** | `Snail Paced Survival` | — | — | — | **完了**: 極低速(0.1-0.5)で「生存」を刻み込み、完走の基礎を構築。 |
| **EXP-37** | `270° Specification` | — | — | — | **進行中**: 観測空間を 270° に統一し、tamoku マップにて Fresh 学習開始。 |
| **EXP-38** | `40Hz Sync` | — | 0.35 | 0.998 | **完了**: 実機LiDARに合わせシミュレーションを40Hzに同期。制御の緻密さが向上。 |
| EXP-39 | `High-Res & Stack` | — | 0.32 | 0.999 | **完了**: LiDAR解像度倍増(1440本) & フレーム積層(x4)導入。ONNX出力完了。 |
| EXP-40 | `CNN Verify (2.5m/s)` | — | — | — | **準備完了**: developmentブランチのCNNポリシー検証。手動実行待ち。 |


---

## 4c. EXP-28 失敗分析 (Precision Turn)

### 動画で観察された現象
車体はほとんど曲がらずに**直進して壁に衝突**する。ステアリング出力がほぼ0に固定されている。

### 根本原因：ステア連動ペナルティの逆効果

EXP-28で追加した「大舵角時の速度抑制」報酬の設計が致命的に誤っていた。

```python
# 意図: コーナー中は速度を出しすぎるな
# 実際: 大舵角ほど損 → AIはハンドルを切らなくなった
if front_dist < 2.0:
    steer_intensity = abs(action[0])
    penalty_scale = 2.0 * (1.0 + steer_intensity * 3.0)
    # steer=0.0 → penalty=2.0 (小)
    # steer=1.0 → penalty=8.0 (4倍！)
```

AIは「壁に近づいたらハンドルを切らない = ペナルティ最小」という**最適戦略を正確に学習**した結果、直進→壁衝突を繰り返した。

### 複合要因
- `MIN_SPEED=1.0`: コーナーで物理的に曲がりきれない速度域が存在
- `LIDAR_MEAN/STD`: 解像度変更(DOWNSAMPLE 10→5)後も旧値のまま（軽微）

---

## 4e. EXP-29 失敗分析 (Straight Fix)

### 結果: 完走率 0%, std=20.4 (0.37→完全発散)

| 指標 | 値 |
|---|---|
| **完走率** | **0%** (20/20 衝突) |
| **std (初期→終期)** | **0.37 → 20.4** |
| explained_variance | 0.992 (良好) |
| avg_speed | 0.489 m/s (≈ MIN_SPEED=0.5) |

### 根本原因: ステアペナルティの副作用が失われた

EXP-28のステアペナルティは「直進学習」という悪い学習を生んだが、**副作用として行動のnstdを ~0.96 に抑制する忘れてはならない機能**を持っていた。
EXP-29でそれを削除したことで、速度ペナルティの制約が大幅に緩まり、stdが制御不能になった。

### EXP-26〜EXP-29 失敗パターンの構造的問題

```
EXP-25: MIN=0.3, MAX=2.5, Fresh → 100% ✅ (ブレーキ許可で曲がれる)
EXP-26: MIN=1.0, MAX=4.0, Fresh →   0% ❌ (ブレーキ封印で曲がれない)
EXP-27: EXP-26+探索強化, Resume → 0% ❌ (同上)
EXP-28: EXP-27+ステアペナルティ → 0% ❌ (直進衝突)
EXP-29: EXP-28-ステアペナルティ, Fresh → 0% ❌ (std発散)
```

**結論: 新規学習(Fresh)による高速化は失敗継続中。EXP-25の「曲がる能力」をResumeで引き継ぐ必要がある。**

---

## 4f. EXP-30 詳細プラン (Gradual Speed Up - Clean Resume)

### 目的
EXP-25（100%完走）のモデルをベースに Resumeし、**「曲がる能力」を完全に保持したまま速度のみ引き上げる**。
EXP-25の観渫設定(DOWNSAMPLE=10, FRAME_STACK=4)と完全一致させ、次元不整合無しのクリーンなResumeを実現する。

### 変更点

| 項目 | EXP-25 (ベース) | EXP-30 (提案) | 理由 |
|---|---|---|---|
| **学習方法** | Fresh | **EXP-25からResume** | 「曲がる能力」を失わない |
| `MIN_SPEED` | 0.3 m/s | **0.5 m/s** | 極低速走行への退避を防ぐ |
| `MAX_SPEED` | 2.5 m/s | **3.0 m/s** | 段階的引き上げ（EXP-26の4.0は大きすぎた） |
| `LIDAR_DOWNSAMPLE_FACTOR` | 10 | 10 (維持) | EXP-25と完全一致させる |
| `FRAME_STACK` | 4 | 4 (維持) | EXP-25と完全一致させる |
| ステアペナルティ | なし | なし (維持) | EXP-28の失敗要因を引き継がない |
| 学習ステップ | 10M | **5M** (Resumeなので短く) | 既に曲がれるのでファインチューニングのみ |

### 期待値
- EXP-25の完走率（70%以上）を維持しながら高速化
- 平均速度が 0.29m/s(EXP-25) → 0.4〜0.6m/s へ向上
- stdが 0.3〜0.6 の安定範囲を維持

---

---

## 3. 実験管理ルール (Control Rules)

1. **1実験 = 1実行**: `python3 scripts/train.py` を実行する。
2. **評価の一貫性**: 実行後は必ず `python3 scripts/evaluate.py --episodes 20` を実行し、CSV/JSONログを生成すること。
3. **シミュレーション可視化**: `python3 scripts/enjoy_wide.py` を動かして走行動画（mp4）を生成し、数値データ（完走率・報酬）だけでなく、挙動を視覚的に分析して考察に含めること。
4. **継続の判断**: 改善が見られたら、次の実験（EXP-xx）には進まず、その設定値をベースとしてさらに微調整を行うこと。

---

## 4a. EXP-13 詳細プラン (Brake Before Corner)

### 目的
コーナーで外側に膨らむ根本原因は、`MIN_SPEED = 1.0 m/s` という設計上の床にある。AIが「曲がろうとしている」のに、物理的に曲がりきれない。コーナー手前で適切に減速させることで完走を実現する。

### 結果 (70% 完走成功)
`MIN_SPEED=0.3` とブレーキ報酬の強化により、ついに完走率が 70% に達した。AIはコーナー手前でしっかりと減速し、「安全第一」の走りを展開している。次の課題は、この完走能力を維持したまま、現在 0.29m/s となっている平均速度を向上させることである。

---

## 4b. EXP-14 詳細プラン (Gradual Speed Up)

### 目的
EXP-13で確立した「コーナーでの減速能力」を維持しつつ、直線や緩やかなカーブでのスピードを向上させる。

### 変更点
- **`MIN_SPEED`**: 0.3 m/s (維持 - ブレーキ能力を奪わないため)
- **`REWARD_SPEED_WEIGHT`**: 1.0 -> **2.0** (速く走ることへのインセンティブ強化)
- **`REWARD_PROGRESS_WEIGHT`**: 2.0 -> **3.0** (より早く、遠くへ進むことを促す)

## 4. 今後のアクションプラン (Scenarios)

### 【成功時】完走率 > 50% かつ std < 1.0
- **高速化**: `MAX_SPEED` を段階的に引き上げ (2.5 -> 3.5)。
- **効率化**: `REWARD_SPEED_WEIGHT` を強化し、速く走るほど明確に得をするようにする。

### 【停滞時】完走率は高いが速度が 1.0m/s 固定
- **速度下限報酬**: 1.5m/s 未満で走り続けることへの微小なペナルティを `rewards.py` に実装。
- **脳の拡大**: `NET_ARCH` を [128, 128] に拡大し、複雑な情報の統合能力を高める。

### 【失敗時】どんな設定でも特定の場所でぶつかる
- **観測空間の精査**: ΔLiDARがノイズになっている可能性を疑い、`config.py` でオフにして比較実験を行う。
- **モデルの初期化**: 悪い癖がついている可能性があるため、必ず新規学習（モデルのロードなし）で行う。

---

## 5. 実行コマンド集 (Cheat Sheet)
```bash
# 学習 (モデル名は ID に合わせる)
docker compose exec f1-sim-latest TRAINING_PROFILE=auto python3 scripts/train.py --steps 4000000 --model /workspace/models/ppo_exp05

# 評価
docker compose exec f1-sim-latest python3 scripts/evaluate.py --model ppo_exp05 --episodes 20

# 可視化 (動画生成)
docker compose exec f1-sim-latest python3 scripts/enjoy_wide.py --model ppo_exp05 --save /workspace/gif/ppo_exp05.mp4

# リアルタイム監視
tensorboard --logdir logs --host localhost
```
---

## 4g. EXP-40 詳細プラン (CNN Policy Verification)

### 目的
developmentブランチで実装された `Conv1DLidarExtractor` (CNNポリシー) と高解像度LiDAR観測が正しく機能するかを検証する。高速化の前に、まずは 2.5m/s での「安定した完走能力」を CNN で再構築できるかを確認する。

### 変更点

| 項目 | EXP-39 (ベース) | EXP-40 (CNN検証) | 理由 |
|---|---|---|---|
| **学習方法** | Fresh | **Fresh Start** | ポリシー構造(MLP->CNN)変更のため |
| **ポリシー** | MLP | **CNN (Conv1D)** | 新機能の動作確認 |
| `MAX_SPEED` | 2.5 m/s | **2.5 m/s** | まずは安定性を確保 |
| `LIDAR_DOWNSAMPLE`| 5 | 5 (維持) | 高解像度での特徴抽出をテスト |
| 学習ステップ | 10M | **10M** | CNNの収束性を十分確認 |

### 期待値
- Conv1D により、壁の形状やコーナーの連続性をより良く捉え、MLPよりも滑らかな走行が実現されること。
- `explained_variance` が早期に上昇し、安定した完走率が得られること。
