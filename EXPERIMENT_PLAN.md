# F1Tenth RL 改善実験計画書

> **作成日**: 2026-03-15  
> **目的**: PPO_27（500万ステップ、完走率0%）の失敗原因に基づき、**少しずつ変数を変えながら原因を特定・改善**する。  
> **根本問題**: 学習終了時に `std`（行動のばらつき）が 1.0→**3.43** に発散し、AIの行動が制御不能になっている。

---

## 診断結果サマリー（PPO_27）

| 指標 | 初期値 | 最終値 | 判定 |
|---|---|---|---|
| `explained_variance` | 0.0003 | 0.2228 | 🟡 低い（上がっているが不十分） |
| `std` | 1.00 | **3.43** | 🔴 **発散（最大の問題）** |
| `value_loss` | 59,678 | 2,697 | 🟢 改善 |
| `entropy_loss` | -2.84 | -5.29 | 🟢 収束傾向 |
| 完走率 | — | **0%** | 🔴 全エピソード衝突 |
| 平均走行ステップ | — | 1,317 | 🔴 最大2000のうち途中で衝突 |

**行動パターンが4種類しかない（スタート位置ごとに毎回同じ壁で衝突）** = 特定の行動パターンに固着しながらも、それが発散している矛盾した状態。

---

## 実験一覧

実験は**1つずつ**順番に実施する。前の実験のベンチマーク結果を見てから次に進むこと。

---

### 実験 EXP-01：`std` 発散の抑制（最優先）

**仮説**: `log_std` の上限が無制限であるため行動ばらつきが発散している。  
**変更点**: `train.py` の PPO 初期化に `log_std_init` と `log_std_max` を追加。

#### 変更ファイル: `scripts/train.py`
```python
# 変更前
model = PPO(
    "MlpPolicy",
    env,
    learning_rate=config.LEARNING_RATE,
    ent_coef=config.PPO_ENT_COEF,
    policy_kwargs=dict(net_arch=config.NET_ARCH),
    ...
)

# 変更後
model = PPO(
    "MlpPolicy",
    env,
    learning_rate=config.LEARNING_RATE,
    ent_coef=config.PPO_ENT_COEF,
    policy_kwargs=dict(
        net_arch=config.NET_ARCH,
        log_std_init=-1.0,    # 初期ばらつきを小さめに（デフォルト: 0.0）
    ),
    ...
)
```

#### 判定基準
| 指標 | 目標値 |
|---|---|
| 最終 `std` | **< 1.5** に収まること |
| 完走率 | 0% より上であること |

---

### 実験 EXP-02：エントロピー係数の調整

**EXP-01 実施後、完走率がまだ 0% の場合のみ実施。**  
**仮説**: `PPO_ENT_COEF=0.01` が高すぎて、収束を妨げている。

#### 変更ファイル: `src/config.py`
```python
# 変更前
PPO_ENT_COEF = 0.01

# 変更後
PPO_ENT_COEF = 0.003  # 探索を絞って収束を促す
```

#### 判定基準
| 指標 | 目標値 |
|---|---|
| `entropy_loss` の最終値 | -5.29 より小さい（絶対値で）こと |
| 完走率 | 0% より上であること |

---

### 実験 EXP-03：衝突ペナルティの緩和

**EXP-02 実施後も完走率が 0% の場合のみ実施。**  
**仮説**: `REWARD_COLLISION=-1000` が大きすぎて、`value_loss` の勾配信号を支配し、価値関数の学習を妨げている。

#### 変更ファイル: `src/config.py`
```python
# 変更前
REWARD_COLLISION = -1000.0

# 変更後
REWARD_COLLISION = -200.0  # ペナルティを緩和
REWARD_SURVIVAL  = 0.1     # 生存報酬を少し増やして補完
```

#### 判定基準
| 指標 | 目標値 |
|---|---|
| `explained_variance` 最終値 | 0.5 以上 |
| 平均走行ステップ | 1,800 以上（完走に近い） |

---

### 実験 EXP-04：ネットワーク拡張

**EXP-03 実施後も改善が見られない場合のみ実施。**  
**仮説**: `[64, 64]` では1082次元の観測空間（LiDAR+状態）を表現するには容量が不足している。

#### 変更ファイル: `src/config.py`
```python
# 変更前
NET_ARCH = [64, 64]

# 変更後
NET_ARCH = [128, 128]  # ネットワーク幅を2倍に
```

> **注意**: 学習時間が約 1.5〜2 倍になる可能性がある。

---

## 実験管理ルール

1. **1実験 = 1回の `python3 scripts/train.py` 実行**
2. 実験終了後は必ず `python3 scripts/evaluate.py --episodes 20` でベンチマークを取得する。
3. 結果はTensorBoardの `std` と `explained_variance` の最終値と、ベンチマークの完走率で判定する。
4. **改善したら次の実験には進まない**（原因が特定できたところで調整を重ねる）。
5. 2つ以上の変数を同時に変えない。

---

## 実験結果記録

| 実験 | 変更内容 | 完走率 | 最終`std` | `exp_var` | 判定 |
|---|---|---|---|---|---|
| PPO_27（ベースライン） | 変更なし | 0% | 3.43 | 0.22 | ✗ |
| EXP-01 | `log_std_init=-1.0` | — | — | — | 実施予定 |
| EXP-02 | `ENT_COEF=0.003` | — | — | — | 待機 |
| EXP-03 | `COLLISION=-200, SURVIVAL=0.1` | — | — | — | 待機 |
| EXP-04 | `NET_ARCH=[128,128]` | — | — | — | 待機 |
